#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
ll num(ll  a)
{   
    for(int i=1;i<=a;i++)
     {cout<<i;
      if(i!=a)
       cout<<" ";
      }
}
int main()
{ ll a;
   cin>>a;
   num(a);
}
